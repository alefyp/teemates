import styles from "../Landing.module.scss";

const PowerSection = () => {
  return (
    <section className={styles.container__power}>
      <h2>HEADS UP!</h2>
      <p>
        A team composition should be made up of 5 champions that fulfill the
        function for each fundamental role. In the champions option you can add
        your favorites for the selection phase and determine if your team has
        everything to win ...or troll.
      </p>
    </section>
  );
};

export default PowerSection;
